# Jarvis — local voice assistant dependencies (Windows / Python 3.12)
# Brain: Ollama (separate install — see README). These are the Python deps.

requests>=2.31          # Ollama HTTP API + misc
numpy>=1.26             # audio buffers
sounddevice>=0.4.6      # microphone capture
faster-whisper>=1.0.3   # offline speech-to-text (CTranslate2, no torch)
pyttsx3>=2.90           # offline text-to-speech (Windows SAPI5)
comtypes>=1.2.0         # required by pyttsx3 sapi5 driver on Windows
ddgs>=6.0               # DuckDuckGo web search (no API key); successor to duckduckgo_search
